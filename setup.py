from distutils import setup
setup(
    name == 'Zebot2',
    version == '0.1.0',
    description == 'A rewrite of the original Zebot',
    author == 'Muhsin Kumbay',
    author_email == 'muhsinkumbay99@gmail.com'
)
